module IOUtil

import Prelude (vtype)

def return = returnIO
def bind = bindIO

def seq {A B} (a : IO A) (b : IO B) : IO B =
  bind a \_ => b

def map {A B : vtype} (f : A -> B) (io : IO A) : IO B =
  bind io \x => return (f x)
